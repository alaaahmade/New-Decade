// import {
//   getMyChatsService,
//   getChatDataService,
//   createChatService,
// } from '../services/index.js';

// export const getMyChats = async (req, res) => {
//   const { response } = await getMyChatsService(req);
//   res.status(response.status).json(response);
// };

// export const getChatData = async (req, res) => {
//   const { response } = await getChatDataService(req);
//   res.status(response.status).json(response);
// };

// export const createChat = async (req, res) => {
//   const { response } = await createChatService(req);
//   res.status(response.status).json(response);
// };
