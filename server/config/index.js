import 'dotenv/config';

export const {
  PORT,
  MONGO_URI,
  SECRET_KEY,
  ADMIN_LOGIN,
  ADMIN_NAME,
  ADMIN_USERNAME,
  ADMIN_PASSWORD,
  VITE_SECRET_KEY,
  SEVER_SECRET_KEY,
  HOST_NAME,
} = process.env;
